galaxy_info:
  author: Augusto Mendoza (amendoza@navent.com)
  description: your description
  company: Navent

  min_ansible_version: 2.6

  galaxy_tags: [resin,clean,disk,drive,space,free]

# NOTA: todas las dependencias deben estar tambien en el "requirements.yml" del proyecto o en el directorio de roles, sino tirara un error raro en estas lineas.
dependencies: 
  - 
    src: https://github.com/ITNavent/ansible-clean-linux-instance
    version: v1.0.1
    name: clean-linux-instance
    tags: [dependencies] # ponerle un tag a cada dependencia evita que se ejecute automaticamente antes de iniciar el role, y pase a ser el role quien determine en que momento se ejecuta.

# Doc: https://osric.com/chris/accidental-developer/2018/03/installing-ansible-role-dependencies/
